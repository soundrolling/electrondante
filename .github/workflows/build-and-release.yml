name: Build and Release

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags like v1.0.0
  workflow_dispatch:  # Allow manual triggering

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Python (for native module builds)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'  # Python 3.11 has distutils, needed for node-gyp

      - name: Install Python setuptools (for distutils compatibility)
        run: |
          python -m pip install --upgrade pip
          pip install setuptools

      - name: Install dependencies
        run: npm ci

      - name: Rebuild native modules
        run: npm run rebuild || echo "Rebuild failed, electron-builder will handle it"
        continue-on-error: true

      - name: Import code signing certificate
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
        if: env.CSC_LINK != ''
        run: |
          if [ -n "$CSC_LINK" ]; then
            echo "📦 Importing code signing certificate..."
            # Create keychain
            security create-keychain -p "" build.keychain
            security list-keychains -s build.keychain
            security default-keychain -s build.keychain
            security unlock-keychain -p "" build.keychain
            security set-keychain-settings -t 3600 -u build.keychain
            
            # Import certificate
            echo "$CSC_LINK" | base64 --decode > certificate.p12
            security import certificate.p12 -k build.keychain -P "$CSC_KEY_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security || true
            security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "" build.keychain || true
            
            # Verify certificate
            security find-identity -v -p codesigning build.keychain
          fi

      - name: Build macOS installer
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Code signing and notarization
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          CSC_INSTALL_KEYCHAIN_NAME: "build.keychain"
        run: |
          # Export environment variables for notarization (electron-builder needs these)
          export APPLE_ID="$APPLE_ID"
          export APPLE_APP_SPECIFIC_PASSWORD="$APPLE_APP_SPECIFIC_PASSWORD"
          export APPLE_TEAM_ID="$APPLE_TEAM_ID"
          
          # Verify notarization credentials are set
          if [ -n "$APPLE_ID" ] && [ -n "$APPLE_APP_SPECIFIC_PASSWORD" ] && [ -n "$APPLE_TEAM_ID" ]; then
            echo "✅ Notarization credentials found"
          else
            echo "⚠️  Notarization credentials missing - app will be signed but not notarized"
          fi
          
          # Only sign if certificate is provided
          if [ -z "$CSC_LINK" ]; then
            echo "⚠️  Code signing skipped - CSC_LINK not set. Building unsigned app."
            npm run build:mac -- --publish never
          else
            echo "✅ Code signing and notarization enabled - building signed and notarized app"
            npm run build:mac -- --publish never
          fi

      - name: Verify code signing and notarization
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
        if: env.CSC_LINK != ''
        run: |
          # Find the DMG file
          DMG_FILE=$(find dist -name "*.dmg" -type f | head -1)
          if [ -z "$DMG_FILE" ]; then
            echo "⚠️  No DMG file found"
            exit 0
          fi
          
          echo "📦 DMG file: $DMG_FILE"
          
          # Mount the DMG to check the app
          hdiutil attach "$DMG_FILE" -quiet -nobrowse -mountpoint /tmp/dmg_mount || true
          
          # Find the app inside the DMG
          APP_PATH=$(find /tmp/dmg_mount -name "*.app" -type d | head -1)
          
          if [ -n "$APP_PATH" ]; then
            echo "📱 App path: $APP_PATH"
            
            # Verify code signing
            echo "🔍 Verifying code signature..."
            codesign -vvv --deep --strict "$APP_PATH" || echo "⚠️  Code signature verification failed"
            
            # Check notarization ticket
            echo "🎫 Checking notarization ticket..."
            stapler validate "$APP_PATH" && echo "✅ Notarization ticket is stapled" || echo "⚠️  Notarization ticket not found or not stapled"
            
            # Check Gatekeeper assessment
            echo "🛡️  Checking Gatekeeper assessment..."
            spctl -a -vv "$APP_PATH" && echo "✅ Gatekeeper assessment passed" || echo "⚠️  Gatekeeper assessment failed"
            
            # Unmount
            hdiutil detach /tmp/dmg_mount -quiet || true
          else
            echo "⚠️  No app found in DMG"
            hdiutil detach /tmp/dmg_mount -quiet || true
          fi

      - name: List dist directory
        run: ls -la dist/ || echo "dist directory not found"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-installer
          path: dist/*.dmg
          retention-days: 30
          if-no-files-found: warn

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write  # Required to create releases and upload assets
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: |
          echo "Artifacts directory contents:"
          ls -la artifacts/ || echo "artifacts directory not found"
          echo "mac-installer directory contents:"
          ls -la artifacts/mac-installer/ || echo "mac-installer directory not found"

      - name: Create Release and Upload Assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          
          # Find DMG file
          DMG_FILE=$(find ./artifacts -name "*.dmg" -type f | head -1)
          
          if [ -z "$DMG_FILE" ]; then
            echo "No DMG file found in artifacts"
            exit 1
          fi
          
          echo "Found DMG file: $DMG_FILE"
          
          # Create or update release
          if gh release view "$TAG_NAME" > /dev/null 2>&1; then
            echo "Release $TAG_NAME already exists, uploading asset..."
            gh release upload "$TAG_NAME" "$DMG_FILE" --clobber
          else
            echo "Creating new release $TAG_NAME..."
            gh release create "$TAG_NAME" \
              --title "Release $TAG_NAME" \
              --notes "Dante Audio Client Release $TAG_NAME" \
              "$DMG_FILE"
          fi

