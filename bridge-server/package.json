{
  "name": "dante-bridge-server",
  "version": "1.2.1",
  "description": "Bridge server for Dante Personal Monitor Mixer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --legacy-peer-deps",
    "postinstall": "echo 'Dependencies installed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8",
    "bcrypt": "^6.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.14.2"
  },
  "peerDependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
