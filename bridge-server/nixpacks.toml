[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Use npm install instead of npm ci to avoid lock file sync issues with root package.json
# Use --omit=dev to avoid npm warning about production config
cmds = ["npm install --legacy-peer-deps --omit=dev"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"

